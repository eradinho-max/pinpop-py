{
  "name": "pinpop-store",
  "version": "2.2.1",
  "description": "PINPOP • Catálogo de pins con WhatsApp, stock y Admin con 2FA autoconfigurable sobre Netlify Blobs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check:security": "node scripts/security-check.js",
    "check:syntax": "node -c server.js && node -c database.js && node -c storage.js && node -c public/app.js && node -c netlify/functions/api.js",
    "check:release": "node scripts/release-check.js",
    "check:all": "npm run check:syntax && npm run check:security && npm run check:release"
  },
  "keywords": [
    "pins",
    "crocs",
    "jibbitz",
    "charms",
    "ecommerce",
    "whatsapp",
    "inventory"
  ],
  "author": "PINPOP",
  "license": "ISC",
  "dependencies": {
    "@netlify/blobs": "11.1.0",
    "cors": "2.8.6",
    "dotenv": "18.0.1",
    "express": "5.2.1",
    "express-rate-limit": "8.7.0",
    "helmet": "8.3.0",
    "multer": "2.4.0",
    "serverless-http": "3.2.0",
    "qrcode": "1.5.4"
  },
  "engines": {
    "node": ">=22"
  }
}
